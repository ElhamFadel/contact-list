import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';
import {ContactListItem} from './src/components';

const App = () => {
  return <ContactListItem />;
};

export default App;
